#!/usr/bin/env bash
set -euo pipefail

if (($# == 0)); then
	set -- --new-window https://www.google.com/
fi

exec /usr/bin/google-chrome-stable \
	--ozone-platform=wayland \
	--disable-features=Vulkan \
	"$@"
